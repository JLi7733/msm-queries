<!-- app/views/directors/youngest.html.erb -->
<h1>Oldest Director</h1>

<p>The oldest director in our dataset right now is <%= @director.name %>, born on <%= @director.dob.strftime("%B %d, %Y") %>.</p>

<p>(<a href="http://strftime.net/" target="_blank">http://strftime.net/</a> is handy for putting together the formatting strings for the <code>.strftime</code> method.)</p>
